# Input Type Split

## Setup

1. Install PostgreSQL.
2. Restore the provided database dump.
3. Run the SQL queries to get the number of text and voice users.
4. Update the values in `script.js`.
5. Open `index.html` in a browser or using Live Server.

## Technologies Used

- HTML
- CSS
- JavaScript
- Chart.js
- PostgreSQL